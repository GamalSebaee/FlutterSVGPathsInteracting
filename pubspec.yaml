name: pathsInteracting
description: Flutter SVG Paths Interacting

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.5
  flutter:
    sdk: flutter
  path_parsing: ^1.0.1
  touchable: ^1.0.2
  xml: ^6.2.2
  interactable_svg: ^0.0.1

dependency_overrides:
  collection: ^1.17.1

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - images/
